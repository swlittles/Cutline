name: macOS tests
on:
  push:
    branches: [main, dev]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true
jobs:
  native-tests:
    runs-on: macos-15
    timeout-minutes: 35
    env:
      CUTLINE_RESULTS_DIR: ${{ github.workspace }}/test-results
    steps:
      - uses: actions/checkout@v6
      - name: Install test dependencies
        run: brew install xcodegen ffmpeg screenresolution
      - name: Configure virtual UI test desktop
        run: bash scripts/ci-display.sh
      - name: Validate release tooling
        run: python3 -m unittest discover -s Tests/ReleaseTests -v
      - name: Check native keyboard focus regressions
        run: CUTLINE_RESULTS_DIR="$CUTLINE_RESULTS_DIR/keyboard" ./scripts/test-all.sh smoke
      - name: Run unit, media integration, and native UI tests
        run: CUTLINE_RESULTS_DIR="$CUTLINE_RESULTS_DIR/full" ./scripts/test-all.sh
      - name: Package release app
        run: ./scripts/build-app.sh
      - name: Preserve test results and failure screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: macOS-test-results
          path: test-results/
          retention-days: 14
